{
  "scripts": {
    "zdev": "concurrently --kill-others \"npm run bundle-watch\"",
    "build": "node build.mjs",
    "dev": "node buildWatch.mjs"
  },
  "devDependencies": {
    "@asn.aeb/esbuild-css-modules-plugin": "^0.1.9",
    "concurrently": "^9.1.2",
    "esbuild": "0.24.0",
    "tailwindcss": "^3.4.14"
  }
}
